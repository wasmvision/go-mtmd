name: Linux
on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.1'
      - name: Download llama.cpp
        uses: robinraju/release-downloader@v1
        with:
            repository: 'ggml-org/llama.cpp'
            latest: true
            fileName: 'llama-*-bin-ubuntu-x64.zip'
            out-file-path: 'lib'
            extract: true
      - name: Move llama.cpp files to root directory
        run: cp ./lib/build/bin/* .
      - name: Add llama.cpp libs to path
        run: |
            echo "$GITHUB_WORKSPACE" >> "$GITHUB_PATH"
            echo "YZMA_TEST_LIBS=$GITHUB_WORKSPACE" >> "$GITHUB_ENV"
      - name: Run tests
        run: go test -v ./...
      - name: Download test model
        run: |
            mkdir -p ./models
            curl -Lo ./models/SmolLM-135M.Q2_K.gguf https://huggingface.co/QuantFactory/SmolLM-135M-GGUF/resolve/main/SmolLM-135M.Q2_K.gguf
      - name: Run inference test
        run: go run ./examples/hello
